# Flask-SerpAPI-export-tests
Here are two levels you can use on GitHub.  Short (GitHub About / one line) Small Flask app: search Google organic results via SerpAPI, normalize them, and download as JSON (structured, machine-readable). Includes unit tests.
